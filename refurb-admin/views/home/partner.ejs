<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title><%= title || 'Partner With Us' %></title>
  <link rel="stylesheet" href="/home.css?v=<%= cacheBuster %>">
</head>
<body>
  <!-- Header -->
  <header class="home-header">
    <div class="logo">🤝 Partner With Us</div>

    <nav class="nav">
      <a href="/" class="<%= active==='home' ? 'active' : '' %>">Home</a>
      <a href="/shop" class="<%= active==='shop' ? 'active' : '' %>">Shop</a>
      <a href="/partner" class="<%= active==='partner' ? 'active' : '' %>">Partner</a>
    </nav>

    <div class="nav-actions">
      <% if (!isAuthed) { %>
        <a class="btn-admin" href="/login">Admin</a>
      <% } else { %>
        <a class="btn-admin" href="/dashboard">Dashboard</a>
        <form method="post" action="/logout" style="display:inline">
          <button class="btn-admin btn-outline" type="submit">Logout</button>
        </form>
      <% } %>
    </div>
  </header>

  <!-- Hero -->
  <main class="hero hero--soft" style="padding-block:56px;">
    <div class="eyebrow">Partner program</div>
    <h1 class="hero-title">Streamlined Asset Disposition for Orgs</h1>
    <p class="hero-sub">
      Tell us about your refresh. We’ll handle chain-of-custody, verified data erasure,
      grading, certification, and reporting—end to end.
    </p>

    <% if (sent) { %>
      <div class="banner success" role="status" aria-live="polite">
        ✅ Thanks! Your request was sent. We’ll follow up shortly.
      </div>
    <% } %>
  </main>

  <!-- Content -->
  <section class="section" style="padding-top:30px;">
    <div class="layout-2col">

      <!-- FORM COLUMN -->
      <form class="form-stack" method="post" action="/partner" novalidate>
        <!-- Org -->
        <div class="card form-card lift">
          <header class="card-head">
            <h3>Organization</h3>
            <p class="sub">Tell us who we’ll be working with.</p>
          </header>

          <div class="field">
            <label>Organization name
              <input class="input" name="org_name" required placeholder="Legal or trading name"/>
            </label>
          </div>

          <div class="grid-3">
            <div class="field">
              <label>Contact person
                <input class="input" name="contact_name" required placeholder="Full name"/>
              </label>
            </div>
            <div class="field">
              <label>Email
                <input class="input" type="email" name="contact_email" required placeholder="name@org.com"/>
              </label>
            </div>
            <div class="field">
              <label>Phone (optional)
                <input class="input" type="tel" name="contact_phone" placeholder="+61 ..."/>
              </label>
            </div>
          </div>
        </div>

        <!-- Devices -->
        <div class="card form-card lift">
          <header class="card-head">
            <h3>Devices You’re Disposing</h3>
            <p class="sub">Select all that apply, add an estimate, and logistics.</p>
          </header>

          <div class="field">
            <div class="label-row">Types of devices</div>
            <div class="chip-grid">
              <label class="chip-check">
                <input type="checkbox" name="types" value="laptops"><span>Laptops</span>
              </label>
              <label class="chip-check">
                <input type="checkbox" name="types" value="desktops"><span>Desktops</span>
              </label>
              <label class="chip-check">
                <input type="checkbox" name="types" value="monitors"><span>Monitors</span>
              </label>
              <label class="chip-check">
                <input type="checkbox" name="types" value="tablets"><span>Tablets</span>
              </label>
              <label class="chip-check">
                <input type="checkbox" name="types" value="phones"><span>Phones</span>
              </label>
              <label class="chip-check">
                <input type="checkbox" name="types" value="other"><span>Other</span>
              </label>
            </div>
          </div>

          <div class="grid-3">
            <div class="field">
              <label>Approx. quantity
                <input class="input" type="number" min="1" name="quantity" placeholder="e.g., 120"/>
              </label>
              <span class="help">A ballpark figure is fine.</span>
            </div>

            <div class="field">
              <label>Pickup or shipment?
                <select class="select" name="logistics">
                  <option value="pickup">Pickup</option>
                  <option value="shipment">Shipment</option>
                  <option value="both">Either / Both</option>
                </select>
              </label>
            </div>

            <div class="field">
              <label>Target timeline
                <select class="select" name="timeline">
                  <option value="asap">ASAP (1–2 weeks)</option>
                  <option value="this_month">This month</option>
                  <option value="this_quarter">This quarter</option>
                  <option value="date_fixed">Fixed date (specify below)</option>
                </select>
              </label>
            </div>
          </div>

          <div class="field">
            <label>Pickup / site locations
              <input class="input" name="locations" placeholder="e.g., Perth CBD; Joondalup; Fremantle"/>
            </label>
          </div>
        </div>

        <!-- Compliance / Notes -->
        <div class="card form-card lift">
          <header class="card-head">
            <h3>Compliance & Notes</h3>
            <p class="sub">Standards, reporting format, constraints, or fixed dates.</p>
          </header>

          <div class="grid-2">
            <label>Erasure policy or compliance requirements
              <textarea class="input" name="compliance" placeholder="e.g., NIST 800-88 aligned, reporting format, chain-of-custody details"></textarea>
            </label>

            <label>Additional notes / fixed dates
              <textarea class="input" name="notes" placeholder="Anything else we should know"></textarea>
            </label>
          </div>
        </div>

        <!-- Actions -->
        <div class="actions-bar">
          <button class="btn ghost" type="reset">Reset</button>
          <button class="cta" type="submit">Send Request</button>
        </div>
      </form>

      <!-- ASIDE COLUMN -->
      <aside class="sticky">
        <div class="card lift">
          <header class="card-head">
            <div class="eyebrow">What you’ll get</div>
            <h3 style="margin:6px 0 0">Clear, verifiable outcomes</h3>
          </header>

          <ul class="benefits">
            <li><span class="icon-circle">🔒</span>Verified data erasure (NIST 800-88 aligned)</li>
            <li><span class="icon-circle">🧾</span>Wipe confirmation & batch report</li>
            <li><span class="icon-circle">🧰</span>Refurbish & functional checks</li>
            <li><span class="icon-circle">🏷️</span>A/B/C grading & transparent pricing</li>
            <li><span class="icon-circle">🔗</span>Chain-of-custody & simple scheduling</li>
          </ul>

          <div class="divider"></div>
          <p class="muted" style="margin:0;">
            We can also support internal redeploy (instead of resale) where it fits your policy.
          </p>
        </div>
      </aside>
    </div>
  </section>

  <footer class="footer">
    <p>© <%= new Date().getFullYear() %> Refurb Admin. All rights reserved.</p>
  </footer>
</body>
</html>

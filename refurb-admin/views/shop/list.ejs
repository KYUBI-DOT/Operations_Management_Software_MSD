<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Shop Devices</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include("../partials/header") %>

  <main class="container">
    <h1>💻 Devices Available for Purchase</h1>

    <% if (devices.length === 0) { %>
      <p>No devices available at the moment. Please check back later.</p>
    <% } else { %>
      <div class="grid">
        <% devices.forEach(d => { %>
          <div class="card">
            <h2><%= d.brand %> <%= d.model %></h2>
            <p><strong>Grade:</strong> <%= d.grade %></p>
            <p><strong>Price:</strong> $<%= d.price || 'TBA' %></p>
            <a href="#" class="btn">Buy Now</a>
          </div>
        <% }) %>
      </div>
    <% } %>
  </main>

  <%- include("../partials/footer") %>
</body>
</html>

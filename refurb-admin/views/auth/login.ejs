<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title><%= title || 'Admin Login' %></title>
  <link rel="stylesheet" href="/home.css?v=<%= cacheBuster %>">
</head>
<body>
  <header class="home-header">
    <div class="logo">🛡️ Admin Access</div>
    <nav class="nav">
      <a href="/">Home</a>
      <a href="/shop">Shop</a>
    </nav>
  </header>

  <main class="hero" style="padding-block:60px;">
    <h1>Admin Login</h1>
    <p>Sign in to manage devices, wipes, grading, and revenue.</p>

    <% if (error) { %>
      <div class="card" style="max-width:460px; margin:12px auto; border-left:4px solid #ef4444;">
        <p class="muted" style="color:#b91c1c;"><%= error %></p>
      </div>
    <% } %>

    <form class="cards" method="post" action="/login" style="max-width:520px; margin:0 auto;">
      <input type="hidden" name="next" value="<%= next || '' %>"/>
      <div class="card" style="gap:8px;">
        <label>Username
          <input class="input" name="username" autocomplete="username" required />
        </label>
        <label>Password
          <input class="input" type="password" name="password" autocomplete="current-password" required />
        </label>
        <button class="cta" type="submit" style="width:100%;">Login</button>
      </div>
    </form>
  </main>

  <footer class="footer">
    <p>© <%= new Date().getFullYear() %> Refurb Admin</p>
  </footer>
</body>
</html>
